<div id="projects">
  <div>
    <h3>Projects</h3>
    <% @projects.each do |proj| %>
      <div class="proj">
        <a class="project_link" href="#"><%= proj.name %></a>
      </div>
    <% end %>
  </div>
</div>
